# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop_online?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "Xucz202606@!"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      max-lifetime: 1200000
      connection-timeout: 30000

  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      lettuce:
        pool:
          min-idle: 5
          max-idle: 10
          max-active: 20
          max-wait: 3000ms
      timeout: 5000ms

# Redisson 配置
redisson:
  single-server-config:
    address: "redis://localhost:6379"
    database: 0

# 开发环境日志级别
logging:
  level:
    com.shop.online: debug
    org.springframework.security: debug

# Knife4j 开发环境开启
knife4j:
  enable: true
